# Shared config lives at ../../shared/home-lab.json (create from ../../shared/home-lab.example.json)
shared_config_file = "../../shared/home-lab.json"

# Pick which Proxmox host entry to target from shared config.
proxmox_host_key = "pve1"

# Pick a shared network profile from shared config.
network_profile_key = "lan"

pm_api_token_id     = "terraform@pve!grafana"
pm_api_token_secret = "replace-me"

# lxc (preferred) or vm (fallback)
deployment_mode = "lxc"

ip_cidr = "192.168.1.60/24"

root_password  = "replace-me"
ssh_public_key = "ssh-ed25519 AAAA... your-key"
